import React, { PureComponent } from 'react';
import style from './userbox.module.css';

export default class UserBox extends PureComponent {
  render() {
    return (
      <div className={style.userbox}>acc</div>
    )
  }
}
